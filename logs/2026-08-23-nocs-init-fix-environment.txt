Captured: 2026-08-23 ~00:50 UTC+8, over SSH from the laptop
Board:  E4:5F:01:52:55:04, serial 100000004851d437
        Raspberry Pi 4 Model B Rev 1.4, SenseCAP M1 carrier, Wio-WM6108
        (second unit; the first, 57:E7, is the one that also works under OpenMANET)
Image:  Raspberry Pi OS Lite (64-bit), Debian 12 bookworm, kernel 6.6.51+rpt-rpi-v8
Driver: MorseMicro/morse_driver tag mm6108-2.0.1 (commit 98e1936) with
        patches/morse-driver-2.0.1-rpi-spi.patch

RESULT: THE 2-BIT SKEW IS SOLVED. Root cause found, fix implemented and verified
        by a single-parameter A/B inside one driver binary. A follow-up section at
        the end explains why OpenMANET never needed the fix. The CMD53 write path
        is a separate problem and is still open.


================================================================
ROOT CAUSE
================================================================
The MM6108 needs roughly 74 clock cycles with chip select *deasserted* to enter
SPI mode. Morse state this directly in their i.MX93 porting thread:

  "in order to put it into SPI mode, the host needs to toggle the SPI clock line
   ~74 times while the CS pin is held high - ie, inverted compared to normal
   operation."

and in the same thread, of a host that failed exactly the way this one did:

  "The original configuration had the chip select driven low during
   initialization, preventing the device from responding to subsequent commands."

morse_spi_initsequence() tries to do this by flipping SPI_CS_HIGH around the
training burst. On a cs-gpios controller that does not work: spi_setup() forces
SPI_CS_HIGH back on. Measured directly with the mode logging added to the patch:

  init: mode=0x4 cs_high_default=1 train=18 flip=1
  init: CS deasserted for training, mode=0x4     <-- expected 0x0
  init: CS polarity restored, mode=0x4

0x4 is SPI_CS_HIGH, and it is still set immediately after
`spi->mode &= ~SPI_CS_HIGH; spi_setup(spi);`. So the 74 clocks go out with the
chip *selected*, the chip never enters SPI mode, and every response afterwards
sits two bit times off the byte grid -- `c0 7f` where `01 ff` is expected.

That is the whole thing. Not the kernel tree, not the device tree, not the clock,
not the pin pulls, not power sequencing -- all of which were eliminated first,
across fifteen tests, because the mode logging that revealed it was only added
late and its significance was not recognised until Morse's own wording turned up.


================================================================
THE FIX
================================================================
SPI_NO_CS achieves what the SPI_CS_HIGH flip cannot: the controller leaves the
chip select line alone for the transfer, so the GPIO stays at its inactive level
-- high -- for the whole burst.

  spi->mode |= SPI_NO_CS;
  if (spi_setup(spi) == 0 && (spi->mode & SPI_NO_CS)) {
          memset(mspi->data, 0xFF, MM610X_BUF_SIZE);
          morse_spi_xfer(mspi, train);      /* CS stays deasserted */
          spi->mode = saved_mode;
          spi_setup(spi);
  }

Guarded by a new module parameter `spi_init_no_cs`, default on, with the old
flip retained as a fallback if the controller rejects SPI_NO_CS.

Order matters, and this was the detail that hid the answer for an hour: the burst
has to happen after reset and before any other transaction. Once the chip has
been addressed with CS asserted it does not recover, and a later burst does not
fix it. An earlier userspace attempt did the training *after* a first CMD0 and
saw no improvement, which looked like a negative result and was not.


================================================================
VERIFICATION
================================================================
1. Userspace, spidev bound via driver_override, CS driven by hand with pinctrl
   and SPI_NO_CS set so the controller could not interfere. Each trial preceded
   by a full reset so no state carried over:

     CS held HIGH throughout, CMD0     -> ff ff ff ff   (not selected, silent --
                                                         confirms CS really is
                                                         under manual control)
     CS LOW, CMD0, no training         -> ff c0 7f ff   R1=0x01 @bit10  SKEWED
     80 clocks @ CS HIGH, then CMD0    -> ff 01 ff ff   R1=0x01 @bit8   ALIGNED

   Six consecutive independent runs of reset + training + CMD0: 6/6 aligned.

   Full command set after a correct init, all at @bit8:
     CMD0 correct CRC  -> 0x01   idle
     CMD0 bad CRC      -> 0x09   idle + CRC error
     CMD13             -> 0x05   idle + illegal command
     CMD63             -> 0x01

2. In the driver, single parameter changed, same binary, same board:

     spi_init_no_cs=Y (default)
       init: training with SPI_NO_CS, mode=0x44
       no `morse rx` skew lines at all, CMD63 passes
       Loaded firmware from morse/mm6108.bin, crc32 0xbe7b5c8f
       Loaded BCF from morse/bcf_fgh100mhaamd.bin, crc32 0x941b2a82
       log: 2026-08-23-bookworm-6.6.51-nocs-fix-dmesg.log

     spi_init_no_cs=N (previous behaviour)
       init: CS deasserted for training, mode=0x4    <-- flip silently fails
       morse rx: c0 3f / c0 7f, CMD63 ret:-71
       log: 2026-08-23-bookworm-6.6.51-nocs-off-dmesg.log

3. `spi_rx_lshift` is no longer needed. Every earlier run required
   spi_rx_lshift=2 even to read the chip ID; these runs pass none, and the read
   path works natively -- 468 KB of firmware and the BCF both transfer correctly.


================================================================
WHAT IS STILL BROKEN
================================================================
CMD53 writes still fail, but the failure has changed character and is clearly a
different problem:

  before the fix   519 bytes clocked after CRC, every one 0xff. The chip was
                   silent. b=0xffffffff.
  after the fix    the chip answers. b=0x001f0002. The failure moved from
                   `fn=1 0x00004050:4` to `fn=2 0x00000000:14`, which is the
                   firmware download, so init gets substantially further.

With the stock ack search, the first non-0xff byte in the 3440-byte window of
that multi-block write is 0xeb at +261, which is not an accept token.

Morse's own OpenWrt feed patch changes find_data_ack to scan for an accept token
rather than stop at the first non-0xff byte. That is now implemented behind
`spi_ack_scan`, default on. On this board **it changes nothing**: the scan runs
the full 3440 bytes and finds no 0x05. Recorded because it is vendor-sanctioned
and someone will otherwise try it again.

Also fixed here: with scanning on, "no non-0xff byte in N bytes" was the wrong
message -- reaching the end of the window in scan mode means no accept token was
found, which says nothing about whether the chip answered. The two now read
differently.

Sweeping spi_post_write_status_bytes 0/1/2/3/6 moves how far init gets: 0 and 1
stop at `fn=1 0x00004050:4`, while 2 and above reach the `fn=2` firmware
download. Values above 2 make no further difference.


================================================================
FOR ISSUE #9
================================================================
This is the answer to the failure in the title of that thread, and it is not
specific to this carrier: any host where spi_setup() forces SPI_CS_HIGH on for a
cs-gpios device gets a training burst delivered with the chip selected, and the
chip then never enters SPI mode. That is every mainline kernel and every rpi
kernel carrying 950-0204. The fix belongs in the driver.


================================================================
FOLLOW-UP — why OpenMANET never needed the fix, and what RESET does
================================================================
  Board E4:5F:01:52:55:04, userspace, spidev via driver_override, SPI_NO_CS set
  and CS driven by hand with pinctrl. Module power is GPIO18 on this carrier, so
  a genuine cold power cycle of the module is possible: drive 18 low, wait, high.

  QUESTION: OpenMANET runs the same driver with the same broken CS flip, so why
  does it not show the offset?

  HYPOTHESIS: its reset-gpios flag is 0 (GPIO_ACTIVE_HIGH), so
  morse_hw_reset()'s gpiod_set_value(reset, 1) drives the pin *high* and RESET_N
  never actually fires. If the chip powers up already in SPI mode it would simply
  stay there, and a broken training burst would not matter.

  RESULT: substantially confirmed, with one caveat.

    cold power cycle, no training at all     -> ff 01 ff   ALIGNED  (3/3)
    cold power cycle, then training          -> ff 01 ff   ALIGNED  (2/2)

    within one power-on:
      1. after cold power-up                 -> ff 01 ff   aligned, in SPI mode
      2. after a RESET_N pulse               -> ff c0 7f   OFFSET, knocked out
      3. training after step 2               -> ff c0 7f   not recovered

  Step 1 -> 2 is the clean one: same power-on, nothing changed but a RESET_N
  pulse, and the chip goes from aligned to offset. **RESET_N takes the chip out
  of SPI mode.** Step 3 is the ordering rule again -- step 2 addressed the chip
  with CS asserted, and after that a training burst does not recover it.

  CAVEAT, and it matters: the power-on state is not perfectly deterministic. A
  second round of the same sequence came up *offset* straight after the cold
  power cycle -- 1 of 5 cold power-ons observed. Cause unknown; candidates are
  how long power was actually off, residual charge on the module, or the level of
  RESET_N / CS during the power ramp. So: the chip *usually* powers up in SPI
  mode, not always.

  A methodology note worth keeping. An earlier version of this test used a 1.5 s
  wait after re-applying power and produced a completely different picture --
  everything offset, including sequences already verified to work. The module
  needs more than 1.5 s after power-up before it will accept anything; 5 s is
  reliable. Re-running a known-good sequence first is what caught it, and without
  that check the artefact would have been archived as a finding.


================================================================
WHAT THIS EXPLAINS
================================================================
OpenMANET, with reset-gpios flag 0, never asserts RESET_N. The chip stays in the
SPI mode it powered up in, so the broken training burst is harmless there. This
repo's overlay uses flag 1, RESET_N genuinely fires, the chip leaves SPI mode,
and the broken burst cannot put it back.

That also explains two community replies that were found early and not understood
at the time (Morse Micro forum, RPi5 SPI thread):

  "the issue resolved after a physical power cycle rather than a soft reboot"
  "most of our deployments use a reset script to toggle the reset line on boot"

A physical power cycle puts the chip back into SPI mode. A soft reboot does not,
because the module keeps power and stays in whatever state the last RESET_N pulse
left it in.

It also explains why testing reset-gpios flag 0 on its own (RUN 7) changed
nothing: by then the chip had already been taken out of SPI mode by earlier
boots, and it was never power-cycled during that test.

THE PRACTICAL POINT: with the SPI_NO_CS fix none of this matters. The driver
delivers the training correctly, immediately after reset, so the chip ends up in
SPI mode regardless of how it powered up or how many times it has been reset.
OpenMANET works by avoiding the problem -- it does not reset the chip -- not by
handling it. The fix makes it deterministic instead of lucky.
